#include <stdio.h> // Include the standard input-output library header file

//The #include directive in C (and C++) is used to include the contents of another file (usually a header file) into your source code file. 

int main() {
    // Print a message to the console
    printf("Hello, world!\n");

    return 0;
}